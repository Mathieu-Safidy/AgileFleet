import { Text } from "react-native";
import { View } from "react-native";

export default function ProfilTemplate() {
    return (
        <View >
            <Text>ProfilTemplate</Text>
        </View>
    );
}